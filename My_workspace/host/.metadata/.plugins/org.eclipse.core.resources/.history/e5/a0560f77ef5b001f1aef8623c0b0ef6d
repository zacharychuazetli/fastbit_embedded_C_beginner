#include <stdio.h>
#include "math.h"

int main()
{
	// int is 4 bytes, so its 8 numbers in hex
	printf("Add: %d\n", math_add(0x0FFF1111, 0x0FFF1111));
	printf("Add in hex: %X\n", math_add(0x0FFF1111, 0x0FFF1111));
	printf("Mul: %d\n", math_mul(0x0FFF1111, 0x0FFF1111));
	return 0;
}
